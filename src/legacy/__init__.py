"""Documentation-only namespace for backwards-compatibility paths.

This package exists to make the supported runtime surface explicit. New code
should prefer the actively maintained modules under `src.training`,
`src.evaluation`, `src.generator.dynamic_schema_generator`, and
`src.aligned_db.nullified_db`.
"""

